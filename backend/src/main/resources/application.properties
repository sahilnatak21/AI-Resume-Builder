# Server Configuration
server.port=${PORT:8080}
server.address=0.0.0.0

# Application Name
spring.application.name=resume-builder

# Logging
logging.level.com.example.resume=INFO
logging.level.org.springframework=WARN

